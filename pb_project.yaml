# Project name
name: idstitcher-report
# Project's yaml schema version
schema_version: 72
# WH Connection to use.
connection: test
# Model folders to use.
model_folders:
  - models
# Entities in this project and their ids.
entities:
  - name: NA
    id_types:
      - na_id
id_types:
  - name: na_id
